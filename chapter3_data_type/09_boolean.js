//                      불리언(boolean)

/*
    불리언은 true와 false 두 가지 값뿐인 데이터 타입이다.

    C같은 일부 언어에서는 불리언 대신 숫자를 사용.
    0은 false, 다른 숫자는 모두 true.

    JS에서도 비슷한 방식을 사용.
    모든 값을 참 같은 값(truthy), 거짓 같은 값(falsy)

    불리언을 쓸 생각이라면 따옴표 안에 넣지 말아야한다.
    문자열 "false"은 참 같은 값이다.
    
    그렇다면 어떻게 써야할까?     */

    //리터럴과 같이 사용해야한다.
    let heating = true;
    let cooling = false;